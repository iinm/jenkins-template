Usage:

```sh
make jenkins_user=admin jenkins_password=$password run  # run & create admin
make jenkins_plugin_file=plugins.txt install-plugins    # install plugins
make username=jenkins password=$password add-user       # add user
```
